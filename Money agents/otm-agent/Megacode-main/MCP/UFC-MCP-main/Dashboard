<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>File Converter MCP</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;700;800&display=swap');

  :root {
    --bg: #0a0a0f;
    --surface: #111118;
    --border: #1e1e2e;
    --accent: #00f5a0;
    --accent2: #00c8ff;
    --accent3: #ff6b6b;
    --accent4: #ffa94d;
    --text: #e8e8f0;
    --muted: #6b6b8a;
    --grid: rgba(255,255,255,0.02);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'Syne', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Grid background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(var(--grid) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid) 1px, transparent 1px);
    background-size: 48px 48px;
    pointer-events: none;
    z-index: 0;
  }

  /* Glow orbs */
  .orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(120px);
    pointer-events: none;
    opacity: 0.12;
    z-index: 0;
  }
  .orb-1 { width: 600px; height: 600px; background: var(--accent); top: -200px; left: -200px; }
  .orb-2 { width: 500px; height: 500px; background: var(--accent2); bottom: -100px; right: -100px; }

  .container {
    position: relative;
    z-index: 1;
    max-width: 1100px;
    margin: 0 auto;
    padding: 48px 24px;
  }

  /* Header */
  header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 64px;
    flex-wrap: wrap;
    gap: 24px;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .logo-icon {
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28px;
    flex-shrink: 0;
  }

  h1 {
    font-size: 32px;
    font-weight: 800;
    letter-spacing: -0.5px;
    line-height: 1;
  }

  h1 span {
    display: block;
    font-size: 13px;
    font-weight: 400;
    font-family: 'Space Mono', monospace;
    color: var(--accent);
    letter-spacing: 2px;
    margin-top: 4px;
    text-transform: uppercase;
  }

  .badge {
    background: rgba(0,245,160,0.1);
    border: 1px solid rgba(0,245,160,0.3);
    color: var(--accent);
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    padding: 6px 12px;
    border-radius: 20px;
    letter-spacing: 1px;
  }

  /* Stats bar */
  .stats-bar {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-bottom: 48px;
  }

  .stat {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 20px;
    position: relative;
    overflow: hidden;
  }

  .stat::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
  }
  .stat-audio::after { background: var(--accent); }
  .stat-video::after { background: var(--accent2); }
  .stat-image::after { background: var(--accent4); }
  .stat-doc::after { background: var(--accent3); }

  .stat-num {
    font-size: 36px;
    font-weight: 800;
    line-height: 1;
    font-family: 'Space Mono', monospace;
  }
  .stat-audio .stat-num { color: var(--accent); }
  .stat-video .stat-num { color: var(--accent2); }
  .stat-image .stat-num { color: var(--accent4); }
  .stat-doc .stat-num { color: var(--accent3); }

  .stat-label {
    font-size: 12px;
    color: var(--muted);
    margin-top: 4px;
    font-family: 'Space Mono', monospace;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  /* Section title */
  .section-title {
    font-size: 11px;
    font-family: 'Space Mono', monospace;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .section-title::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border);
  }

  /* Category cards */
  .categories {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-bottom: 48px;
  }

  .category-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 28px;
    transition: border-color 0.2s, transform 0.2s;
    cursor: pointer;
  }
  .category-card:hover {
    transform: translateY(-2px);
  }
  .category-card.audio:hover { border-color: rgba(0,245,160,0.4); }
  .category-card.video:hover { border-color: rgba(0,200,255,0.4); }
  .category-card.image:hover { border-color: rgba(255,169,77,0.4); }
  .category-card.doc:hover { border-color: rgba(255,107,107,0.4); }

  .cat-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;
  }

  .cat-icon {
    width: 44px;
    height: 44px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
  }
  .audio .cat-icon { background: rgba(0,245,160,0.1); }
  .video .cat-icon { background: rgba(0,200,255,0.1); }
  .image .cat-icon { background: rgba(255,169,77,0.1); }
  .doc .cat-icon { background: rgba(255,107,107,0.1); }

  .cat-count {
    font-family: 'Space Mono', monospace;
    font-size: 12px;
  }
  .audio .cat-count { color: var(--accent); }
  .video .cat-count { color: var(--accent2); }
  .image .cat-count { color: var(--accent4); }
  .doc .cat-count { color: var(--accent3); }

  .cat-name {
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .cat-desc {
    font-size: 13px;
    color: var(--muted);
    line-height: 1.5;
    margin-bottom: 20px;
  }

  .formats-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .fmt-tag {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    padding: 4px 8px;
    border-radius: 4px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    border: 1px solid;
  }
  .audio .fmt-tag { background: rgba(0,245,160,0.05); border-color: rgba(0,245,160,0.2); color: var(--accent); }
  .video .fmt-tag { background: rgba(0,200,255,0.05); border-color: rgba(0,200,255,0.2); color: var(--accent2); }
  .image .fmt-tag { background: rgba(255,169,77,0.05); border-color: rgba(255,169,77,0.2); color: var(--accent4); }
  .doc .fmt-tag { background: rgba(255,107,107,0.05); border-color: rgba(255,107,107,0.2); color: var(--accent3); }

  /* Tools section */
  .tools-list {
    display: grid;
    gap: 12px;
    margin-bottom: 48px;
  }

  .tool-row {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 20px 24px;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 16px;
    align-items: center;
  }

  .tool-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .tool-dot.green { background: var(--accent); box-shadow: 0 0 8px var(--accent); }
  .tool-dot.blue { background: var(--accent2); box-shadow: 0 0 8px var(--accent2); }
  .tool-dot.orange { background: var(--accent4); box-shadow: 0 0 8px var(--accent4); }
  .tool-dot.red { background: var(--accent3); box-shadow: 0 0 8px var(--accent3); }
  .tool-dot.purple { background: #b48ead; box-shadow: 0 0 8px #b48ead; }
  .tool-dot.teal { background: #4ecdc4; box-shadow: 0 0 8px #4ecdc4; }

  .tool-name {
    font-family: 'Space Mono', monospace;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.5px;
  }

  .tool-desc {
    font-size: 13px;
    color: var(--muted);
  }

  .tool-type {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    color: var(--muted);
    border: 1px solid var(--border);
    padding: 3px 8px;
    border-radius: 4px;
    white-space: nowrap;
  }

  /* Install block */
  .install-block {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 32px;
    margin-bottom: 32px;
  }

  .install-block h3 {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .install-block h3 .step-num {
    width: 24px;
    height: 24px;
    background: var(--accent);
    color: var(--bg);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: 800;
    font-family: 'Space Mono', monospace;
  }

  pre {
    background: rgba(0,0,0,0.4);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 16px 20px;
    font-family: 'Space Mono', monospace;
    font-size: 12px;
    line-height: 1.7;
    overflow-x: auto;
    margin-bottom: 16px;
    position: relative;
  }

  pre .comment { color: var(--muted); }
  pre .key { color: var(--accent2); }
  pre .val { color: var(--accent); }
  pre .cmd { color: var(--accent4); }
  pre .str { color: #f1fa8c; }

  .copy-hint {
    font-size: 11px;
    color: var(--muted);
    font-family: 'Space Mono', monospace;
    margin-top: -8px;
    margin-bottom: 16px;
  }

  .steps {
    display: grid;
    gap: 16px;
  }

  .step {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }

  .step-num-sm {
    width: 28px;
    height: 28px;
    border: 1px solid var(--border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--muted);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .step-content {
    flex: 1;
  }

  .step-label {
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .step-sub {
    font-size: 12px;
    color: var(--muted);
    line-height: 1.5;
  }

  /* Footer */
  footer {
    text-align: center;
    padding-top: 48px;
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 1px;
    border-top: 1px solid var(--border);
  }

  footer span { color: var(--accent); }

  /* Responsive */
  @media (max-width: 700px) {
    .stats-bar { grid-template-columns: repeat(2, 1fr); }
    .categories { grid-template-columns: 1fr; }
    h1 { font-size: 24px; }
  }

  /* Animations */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .container > * {
    animation: fadeUp 0.5s ease both;
  }
  .container > *:nth-child(2) { animation-delay: 0.05s; }
  .container > *:nth-child(3) { animation-delay: 0.1s; }
  .container > *:nth-child(4) { animation-delay: 0.15s; }
  .container > *:nth-child(5) { animation-delay: 0.2s; }
  .container > *:nth-child(6) { animation-delay: 0.25s; }
</style>
</head>
<body>
<div class="orb orb-1"></div>
<div class="orb orb-2"></div>

<div class="container">

  <header>
    <div class="logo">
      <div class="logo-icon">⚡</div>
      <h1>
        File Converter MCP
        <span>Universal Media & Document Processor</span>
      </h1>
    </div>
    <div class="badge">v1.0.0 · MCP SERVER</div>
  </header>

  <div class="stats-bar">
    <div class="stat stat-audio">
      <div class="stat-num">9</div>
      <div class="stat-label">Audio Formats</div>
    </div>
    <div class="stat stat-video">
      <div class="stat-num">9</div>
      <div class="stat-label">Video Formats</div>
    </div>
    <div class="stat stat-image">
      <div class="stat-num">10</div>
      <div class="stat-label">Image Formats</div>
    </div>
    <div class="stat stat-doc">
      <div class="stat-num">8</div>
      <div class="stat-label">Doc Formats</div>
    </div>
  </div>

  <div class="section-title">Conversion Categories</div>

  <div class="categories">
    <div class="category-card audio">
      <div class="cat-header">
        <div class="cat-icon">🎵</div>
        <div class="cat-count">9 formats · FFmpeg WASM</div>
      </div>
      <div class="cat-name">Audio</div>
      <div class="cat-desc">Convert stems, tracks, and audio files with full bitrate, sample rate, and channel control. DAW-ready AAF/AIFF support.</div>
      <div class="formats-grid">
        <span class="fmt-tag">MP3</span>
        <span class="fmt-tag">WAV</span>
        <span class="fmt-tag">FLAC</span>
        <span class="fmt-tag">AAC</span>
        <span class="fmt-tag">OGG</span>
        <span class="fmt-tag">M4A</span>
        <span class="fmt-tag">AIFF</span>
        <span class="fmt-tag">OPUS</span>
        <span class="fmt-tag">WMA</span>
      </div>
    </div>

    <div class="category-card video">
      <div class="cat-header">
        <div class="cat-icon">🎬</div>
        <div class="cat-count">9 formats · FFmpeg WASM</div>
      </div>
      <div class="cat-name">Video</div>
      <div class="cat-desc">Full video pipeline: transcode, trim, resize, extract audio, generate GIFs. Supports B-roll export workflows.</div>
      <div class="formats-grid">
        <span class="fmt-tag">MP4</span>
        <span class="fmt-tag">WebM</span>
        <span class="fmt-tag">AVI</span>
        <span class="fmt-tag">MOV</span>
        <span class="fmt-tag">MKV</span>
        <span class="fmt-tag">FLV</span>
        <span class="fmt-tag">WMV</span>
        <span class="fmt-tag">GIF</span>
        <span class="fmt-tag">3GP</span>
      </div>
    </div>

    <div class="category-card image">
      <div class="cat-header">
        <div class="cat-icon">🖼️</div>
        <div class="cat-count">10 formats · Sharp</div>
      </div>
      <div class="cat-name">Image</div>
      <div class="cat-desc">Resize, compress, optimize, rotate, grayscale — and auto-generate full icon sets (16px → 512px) for apps.</div>
      <div class="formats-grid">
        <span class="fmt-tag">JPEG</span>
        <span class="fmt-tag">PNG</span>
        <span class="fmt-tag">WebP</span>
        <span class="fmt-tag">AVIF</span>
        <span class="fmt-tag">GIF</span>
        <span class="fmt-tag">TIFF</span>
        <span class="fmt-tag">ICO</span>
        <span class="fmt-tag">BMP</span>
        <span class="fmt-tag">SVG</span>
      </div>
    </div>

    <div class="category-card doc">
      <div class="cat-header">
        <div class="cat-icon">📄</div>
        <div class="cat-count">8 formats · Pure Node</div>
      </div>
      <div class="cat-name">Documents</div>
      <div class="cat-desc">Bidirectional conversion for dev docs and data interchange. No external cloud — fully offline, no rate limits.</div>
      <div class="formats-grid">
        <span class="fmt-tag">Markdown</span>
        <span class="fmt-tag">JSON</span>
        <span class="fmt-tag">YAML</span>
        <span class="fmt-tag">HTML</span>
        <span class="fmt-tag">CSV</span>
        <span class="fmt-tag">TXT</span>
      </div>
    </div>
  </div>

  <div class="section-title">MCP Tools (6 Total)</div>

  <div class="tools-list">
    <div class="tool-row">
      <div class="tool-dot green"></div>
      <div>
        <div class="tool-name">convert_audio</div>
        <div class="tool-desc">Convert between audio formats with bitrate, sample rate, and channel control</div>
      </div>
      <div class="tool-type">AUDIO</div>
    </div>
    <div class="tool-row">
      <div class="tool-dot blue"></div>
      <div>
        <div class="tool-name">convert_video</div>
        <div class="tool-desc">Transcode video with trim, resize, fps control, audio extraction</div>
      </div>
      <div class="tool-type">VIDEO</div>
    </div>
    <div class="tool-row">
      <div class="tool-dot orange"></div>
      <div>
        <div class="tool-name">convert_image</div>
        <div class="tool-desc">Convert, resize, compress images + auto icon set generation (9 sizes)</div>
      </div>
      <div class="tool-type">IMAGE</div>
    </div>
    <div class="tool-row">
      <div class="tool-dot red"></div>
      <div>
        <div class="tool-name">convert_document</div>
        <div class="tool-desc">Bidirectional: Markdown ↔ JSON ↔ YAML ↔ HTML ↔ CSV</div>
      </div>
      <div class="tool-type">DOCUMENT</div>
    </div>
    <div class="tool-row">
      <div class="tool-dot purple"></div>
      <div>
        <div class="tool-name">batch_convert</div>
        <div class="tool-desc">Convert multiple files at once with the same settings — parallel processing</div>
      </div>
      <div class="tool-type">BATCH</div>
    </div>
    <div class="tool-row">
      <div class="tool-dot teal"></div>
      <div>
        <div class="tool-name">get_supported_formats · get_conversion_history</div>
        <div class="tool-desc">Inspect supported formats by category, review session conversion history</div>
      </div>
      <div class="tool-type">UTILITY</div>
    </div>
  </div>

  <div class="section-title">Installation & Setup</div>

  <div class="install-block">
    <div class="steps">
      <div class="step">
        <div class="step-num-sm">1</div>
        <div class="step-content">
          <div class="step-label">Install dependencies</div>
          <div class="step-sub">Requires Node.js ≥ 18 and ffmpeg on PATH</div>
          <pre style="margin-top:10px"><span class="cmd"># Install ffmpeg (required for audio/video)</span>
brew install ffmpeg           <span class="comment"># macOS</span>
sudo apt install ffmpeg       <span class="comment"># Ubuntu/Debian</span>
winget install Gyan.FFmpeg    <span class="comment"># Windows</span>

<span class="cmd"># Install Node deps</span>
npm install</pre>
        </div>
      </div>
      <div class="step">
        <div class="step-num-sm">2</div>
        <div class="step-content">
          <div class="step-label">Build the server</div>
          <pre style="margin-top:10px"><span class="cmd">npm run build</span></pre>
        </div>
      </div>
      <div class="step">
        <div class="step-num-sm">3</div>
        <div class="step-content">
          <div class="step-label">Add to Claude Desktop config</div>
          <div class="step-sub">Edit <code style="color:var(--accent)">~/Library/Application Support/Claude/claude_desktop_config.json</code></div>
          <pre style="margin-top:10px">{
  <span class="key">"mcpServers"</span>: {
    <span class="key">"file-converter"</span>: {
      <span class="key">"command"</span>: <span class="str">"node"</span>,
      <span class="key">"args"</span>: [<span class="str">"/path/to/file-converter-mcp/dist/index.js"</span>]
    }
  }
}</pre>
        </div>
      </div>
      <div class="step">
        <div class="step-num-sm">4</div>
        <div class="step-content">
          <div class="step-label">Restart Claude Desktop — start converting!</div>
          <div class="step-sub">Ask Claude: <em style="color:var(--accent)">"Convert my beat.mp3 to WAV at 48kHz"</em> or <em style="color:var(--accent)">"Generate an icon set from my logo.png"</em></div>
        </div>
      </div>
    </div>
  </div>

  <div class="section-title">Example Prompts</div>

  <div class="install-block">
    <pre><span class="comment"># Audio</span>
<span class="val">"Convert /music/beat.mp3 to WAV at 48000Hz stereo"</span>
<span class="val">"Batch convert all MP3s in /stems/ to FLAC"</span>

<span class="comment"># Video</span>
<span class="val">"Convert broll.mov to WebM, trim to first 30 seconds"</span>
<span class="val">"Extract audio from interview.mp4 as a WAV file"</span>

<span class="comment"># Image</span>
<span class="val">"Convert logo.png to WebP at quality 90, 800px wide"</span>
<span class="val">"Generate a full icon set from app-icon.png"</span>

<span class="comment"># Documents</span>
<span class="val">"Convert README.md to JSON for my API response"</span>
<span class="val">"Transform data.csv to JSON, then to Markdown table"</span></pre>
  </div>

  <footer>
    <p>FILE CONVERTER MCP &nbsp;·&nbsp; BUILT FOR <span>DIGITAL SCIENCE LAB</span> &nbsp;·&nbsp; CHEETAH FRAMEWORK</p>
    <p style="margin-top:8px">POWERED BY FFMPEG · SHARP · @MODELCONTEXTPROTOCOL/SDK</p>
  </footer>

</div>
</body>
</html>
