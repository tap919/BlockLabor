/**
 * Scale AI client — REMOVED.
 *
 * The Scale AI account (tap4500@gmail.com) is a REQUESTER account that submits
 * tasks and pays workers. It does NOT earn money. All Scale AI integration has
 * been intentionally removed from the OTM Agent scanner and executor.
 *
 * This file is kept as a tombstone to prevent accidental re-import.
 * If Scale AI integration is ever needed again, build a new client from scratch
 * with the API key stored ONLY in environment variables (never hardcoded).
 */

export {};
