import { describe, it, expect, vi, beforeEach } from 'vitest'

describe('checkr-webhook', () => {
  it('rejects non-POST requests', async () => {
    // This is just to test the harness
    expect(true).toBe(true)
  })
})
